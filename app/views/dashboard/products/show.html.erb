<% render partial: 'shared/requires_map_libs' %>

<% content_for :body do %><%= @product.name %> <% end %>


<%= react_component('DashboardBoard', props={'id' => @product.id}) %>

<%= link_to I18n.t('Edit'), edit_dashboard_product_path(@product) %> |
<%= link_to I18n.t('Back'), dashboard_products_path %>