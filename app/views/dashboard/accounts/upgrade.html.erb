<div class="row">
  <%= form_for(@business_profile, {url: dashboard_upgrade_form_path}) do |f| %>
      <div class="col-lg-12 text-center">
        <!--<div class="field col-lg-6">
          <%= f.label :plan_type %><br>
          <%= f.select :plan_type, BusinessProfile.plans_types.map { |key, value| [key.humanize, key] } %>
        </div>-->
        <h6>
          <% if @message %>
              <%= @message %>
          <% end %>

        </h6>
        <div class="text-center">
          <%= f.submit :class => 'btn btn-info btn-fill btn-wd' %>
        </div>
      </div>
  <% end %>
</div>