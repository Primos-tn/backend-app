<%= form_for([:dashboard, @webhook]) do |f| %>
  <% if @webhook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@webhook.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @webhook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
  </div>
  <div class="field">
    <%= f.label :brand_id %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
